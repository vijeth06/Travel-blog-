{
  "name": "travel-blog-deployment",
  "version": "1.0.0",
  "description": "Travel Blog deployment configuration",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}